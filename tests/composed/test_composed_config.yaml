seed: 123456
start_datetime: "2025-03-06T00:00:00"
# Keep the Dask orchestration test fast while still checking two coupling steps.
end_datetime: "2025-03-06T02:00:00"
model_3d:
  backend_class_name: "Model3DBackendMock"
  common:
    name: "uhelna"
    # Force the mock backend to split the two-hour run into one-hour messages.
    time_step_hours: 1

model_1d:
  kalman_class_name: "KalmanMock"
  mock_velocity: 3.0e-4
  site_ids: [1]
  kalman_config:
    measurements_noise_level: 0.0
    measurements_noise_distr_type: gaussian
