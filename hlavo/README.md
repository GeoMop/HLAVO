
# top level scripts

...


# subdirs
- `deep_model` - python interface to the deep model based on modflow6; the resources and model preparation scripts in the same directory
- `ingress` - module with data transformation functions for data ingress -> zarr_fuse
- `kalman` - core of current development, application of the UKF: state, result, 
- `soil_parflow` - parflow based model 
- `soil_py` - own implementation of Richards (not used)

