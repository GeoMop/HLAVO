# Historical Daily Meteo Data

Daily data from nearby stations used to quantify infiltration inputs for deep
model calibration.

This directory is historical-analysis support. It does not own a schema in
`hlavo/schemas`; the active CHMI station/Open-Meteo schema update procedure is
under `../meteo_playground/chmi_stations/`.
